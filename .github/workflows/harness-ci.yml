name: harness / ci
on:
  pull_request:
  push:
    branches: [main]

jobs:
  compose-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Harness doctor
        run: |
          chmod +x scripts/doctor
          ./scripts/doctor
